#include <stdio.h>

/**
 * main - Entry point to the program
 *
 * Return: 0 (if successful)
 */
int main(void)
{
	int num = 0;

	while (num < 10)
	{
		printf("%d");
	}
	printf("\n");

	return (0);
}
