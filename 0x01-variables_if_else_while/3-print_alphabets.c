#include <stdio.h>

/**
 * main - Entry point to the program
 *
 * Return: 0 (if successful)
 */
int main(void)
{
	char lower = 'a';
	char upper = 'A';

	while (lower <= 'z')
	{
		putchar(letter);
		lower++;
	}
	putchar('\n');


	while (upper <= 'Z')
	{
		putchar(upper);
		upper++;
	}
	putchar('\n');
}
